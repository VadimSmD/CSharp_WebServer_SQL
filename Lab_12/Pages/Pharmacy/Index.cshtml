@page
@model Lab_12.Pages.Pharmacy.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PharmacyM[0].PharmacistId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PharmacyM[0].SupplyId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PharmacyM[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PharmacyM[0].Doxod)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PharmacyM) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PharmacistId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SupplyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doxod)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
